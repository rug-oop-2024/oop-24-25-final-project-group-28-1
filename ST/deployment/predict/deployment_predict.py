import streamlit as st

# Debug: st.write("This is: deployment/load/deployment_predict.py")
# Debug: st.write("""\n\n Once the user loads a pipeline, prompt them to provide a 
# Debug: CSV on which they can perform predictions.""")

st.warning("Not yet implemented")
